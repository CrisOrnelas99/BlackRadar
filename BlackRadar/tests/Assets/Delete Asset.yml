info:
  name: Delete Asset
  type: http
  seq: 6

http:
  method: DELETE
  url: http://localhost:8080/api/assets/{{assetId}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
