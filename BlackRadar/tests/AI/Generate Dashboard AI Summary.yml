info:
  name: Generate Dashboard AI Summary
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/dashboard/ai-summary
  auth: inherit
  body:
    type: json
    data: |
      {}

settings:
  encodeUrl: true
  timeout: 90000
  followRedirects: true
  maxRedirects: 5
