info:
  name: Test AI Provider
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/ai/test
  auth: inherit

settings:
  encodeUrl: true
  timeout: 90000
  followRedirects: true
  maxRedirects: 5
