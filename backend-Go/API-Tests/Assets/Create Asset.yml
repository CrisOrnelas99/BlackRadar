info:
  name: Create Asset
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/api/assets
  auth: inherit
  body:
    type: json
    data: |
      {
        "name": "Laptop-02",
        "type": "Device",
        "ipAddress": "10.0.0.11",
        "operatingSystem": "Windows 11",
        "owner": "system_admin",
        "criticality": "High"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
